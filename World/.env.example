# Copy this file to ".env" in the same directory as WorldLinux.exe (Core.BaseDirectory).
# Do not commit ".env" — it may contain secrets.
#
# Analytics: long random secret for hashing account/character identifiers in Logs/analytics/*.jsonl
# UO_MEMENTO_ANALYTICS_ACCOUNT_SALT=your_long_random_secret_here
