# Default language for new accounts and when no player context is available (BCP 47): en or zh-Hans
# With no context, the server attempts DefaultLanguage first, then falls back to FallbackLanguage.
DefaultLanguage=zh-Hans

# Fallback when a translation key is missing in DefaultLanguage
FallbackLanguage=en
